# Excel Data Analyzer (Python Project)

A simple tool that reads Excel files (.xlsx), analyzes the data, shows statistics, generates a chart, and creates a PDF report.

## Features
- Read and display Excel data
- Basic statistics (count, mean, min, max)
- Histogram chart of any numeric column
- Export results to a PDF file

## How to Run
1. Install required libraries:2. Run the script: